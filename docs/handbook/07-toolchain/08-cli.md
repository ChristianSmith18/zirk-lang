# CLI

The official CLI covers `new`, `init`, `run`, `build`, `check`, `test`, `bench`, `format`, `lint`, `prepare`, dependency commands, packaging, publishing, and docs. Output is deterministic and `--json` supports automation.

---

**Previous:** [← Diagnostics](07-diagnostics.md) · **Next:** [ Formatter](09-formatter.md)
