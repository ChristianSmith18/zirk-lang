# Concurrency and Parallelism

Concurrency structures overlapping work; parallelism requests simultaneous CPU execution. A task waiting for I/O is concurrent without consuming a thread, while `parallel` divides CPU work across the runtime pool. Choose by intent, not performance folklore.

---

**Previous:** [← Concurrency](./README.md) · **Next:** [Tasks →](./02-tasks.md)
