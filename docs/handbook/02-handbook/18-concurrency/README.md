# Concurrency

Zirk names different work explicitly: `task` for structured concurrent work, `parallel` for CPU work across cores, and `thread` for an operating-system thread. Typed channels and synchronization coordinate them without exposing a global event loop.

---

**Previous:** [← Undefined Behavior](../17-memory-and-safety/11-undefined-behavior.md) · **Next:** [Concurrency and Parallelism →](./01-concurrency-vs-parallelism.md)
