class Counter {
    mut value: Int32;
    construct(value: Int32) { this.value = value; }
      increment(): Void { this.value = this.value + 1; }
}

fn main(): Void {
    inmut::strict c: Counter = Counter(1);
    c.increment();
}
