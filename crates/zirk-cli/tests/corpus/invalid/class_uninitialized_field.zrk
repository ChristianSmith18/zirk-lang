class Inner {
    construct() { }
}

class Outer {
    // Una clase es una referencia con identidad: no hay instancia por defecto,
    // así que el constructor tiene que darle una.
    inner: Inner;

    construct() { }
}

fn main(): Void { }
