// A Float literal that overflows its destination width is rejected at
// compile time (roadmap Phase 3b, task 5.1) — BinaryFloat16's largest finite
// magnitude is 65504; this exceeds it.
fn main(): Void {
    mut a: BinaryFloat16 = 100000.0b16;
}
