// A leaf that cannot convert into the context's target is rejected at
// compile time (roadmap Phase 3b, task 7): `Boolean` has no numeric form.
fn main(): Void {
    mut a: BinaryFloat64 = BinaryFloat64(true + 4);
}
