interface Container<T> {
    fn get(): T;
}

class Box<T> implements Container<T> {
    value: T;
    construct(v: T) { this.value = v; }
    fn get(): Int32 { return this.value; }
}

fn main(): Void {}
