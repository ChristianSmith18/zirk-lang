// Conformidad transitiva: una clase que adopta B, y B implementa A,
// debe satisfacer los métodos de A.
interface A {
     a(): Int32;
}

interface B implements A {
     b(): Int32;
}

class C implements B {
    construct() { }
     a(): Int32 { return 1; }
     b(): Int32 { return 2; }
}

fn main(): Void {
    mut c = C();
    stdout.println(c.a());
}
