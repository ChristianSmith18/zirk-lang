class Counter {
    mut value: Int32;
    construct(v: Int32) { this.value = v; }
}

fn main(): Void {
    mut c: Counter = Counter(5);
    mut pinned: Pin<Counter> = Pin(c);
    // Pin<T> dereferences automatically for field access.
    stdout.println(pinned.value);
}
