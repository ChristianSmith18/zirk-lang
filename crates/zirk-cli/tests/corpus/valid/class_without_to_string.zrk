// A class with no `to_string()` method has no text form (roadmap Phase 3b,
// task 8) — printing one is rejected at compile time.
class User {
    construct() { }
}

fn main(): Void {
    mut u = User();
    stdout.println(u);
}
