class Counter {
    mut value: Int32;
    construct(value: Int32) { this.value = value; }
}

fn main(): Void {
    mut c: Counter = Counter(1);
    unsafe {
        c.value = 42;
    }
    stdout.println(c.value);
}
