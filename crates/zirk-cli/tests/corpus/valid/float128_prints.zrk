// BinaryFloat128 now prints by truncating to BinaryFloat64 for text conversion.
// This is a lossy conversion for values not exactly representable in f64.
fn main(): Void {
    mut a: BinaryFloat128 = 1.5;
    stdout.println(a);
}
