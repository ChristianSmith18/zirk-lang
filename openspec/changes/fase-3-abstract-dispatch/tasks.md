## 1. Checker (zirk-sema)

- [x] 1.1 Remove the `not_lowered` gate for `ClassKind::Abstract` in `check_class` (`crates/zirk-sema/src/checker.rs`, ~line 2474-2483).
- [x] 1.2 Trace and verify the general class-declaration path's method-index/`overridden`-flag assignment (whatever code builds a flattened method list for `ClassKind::Class` and assigns `index`/`overridden`) actually runs for `ClassKind::Abstract` too, and produces a usable shared virtual-index scheme across every concrete adopter — do not assume this "just works" because `CallVirtual` itself is declaration-kind-agnostic (design D1's own explicit warning: the native hierarchy's index assignment was hand-written and may have taken shortcuts the general path doesn't already replicate). **Found it did not "just work"**: (a) a user abstract class's own methods were never marked `overridden: true` (only the three hand-registered native classes were, at native-registration time) — fixed by setting `overridden: decl.kind == ClassKind::Abstract` when a class's own methods are built; (b) the flattened-method-list seeding an adopter needs (previously scoped to exactly `Error`/`Throwable`/`RuntimeError`'s three ids) never ran for a user abstract class at all — generalized to any implemented `ClassKind::Abstract`; (c) `Self::in_hierarchy_order` only ordered by `extends`, not `implements`, so an adopter declared textually before the abstract class it implements would have seeded from that class's still-empty pre-declaration method list — fixed by adding an `implements`-aware readiness check; (d) generalizing the seeding surfaced a latent diagnostic bug in `require_abstract_conformance` (pre-existing for the native classes too, just never exercised by a test): a class that implements an abstract class but supplies no override at all now has a seeded-but-still-unfulfilled entry, which made the "does not implement it" check see something "supplied" and wrongly report `MISSING_OVERRIDE` instead of `MISSING_IMPLEMENTATION` — fixed by filtering out entries whose `owner` is still the abstract class itself.
- [x] 1.3 Verify method-index consistency when one concrete class adopts a user abstract class AND a plain interface/trait in the same `implements` list (design D3/Risk) — both must dispatch correctly without index collisions. Confirmed: an abstract class seeds `methods` (used by `CallVirtual`), while a plain interface/trait is tracked separately in `contracts`/`contract_instances` (used by `CallContract`) — the two never share index space, so there is no collision to resolve.
- [x] 1.4 Checker unit tests added in `crates/zirk-sema/tests/typing.rs`: `valid_abstract_class_alone_is_lowered`, `valid_abstract_class_named_as_a_parameter_type_dispatches`, `valid_two_adopters_of_the_same_abstract_class_get_consistent_indices`, `valid_abstract_class_implemented_before_its_own_declaration`, `valid_class_implements_an_abstract_class_and_an_interface`. Also updated `valid_class_adopts_an_abstract_class` (was incidentally relying on the old `NOT_LOWERED` gate to have *some* error present) and replaced `invalid_abstract_class_is_not_lowered_yet` with `valid_abstract_class_alone_is_lowered`.

## 2. IR (zirk-ir) — verification, not new mechanism (design expects `CallVirtual` needs no change)

- [x] 2.1 Verify a call through a value statically typed as a user abstract class lowers to `InstKind::CallVirtual` (`crates/zirk-ir/src/lower.rs`, ~line 5370-5389) exactly the way a call through `Throwable` already does — confirm by an explicit test, not by reading the code and assuming it applies. Confirmed unchanged: `CallVirtual`/`ir_type`/the object-layout builder are all declaration-kind-agnostic already; test `a_call_through_a_user_abstract_class_goes_through_the_table` added in `crates/zirk-ir/tests/lowering.rs`.
- [x] 2.2 IR-level test: two concrete adopters of the same abstract class, each with a distinct override — confirm the lowered `CallVirtual` correctly resolves to each adopter's own override at its own call site (i.e., the dispatch is receiver-type-driven, not resolved once at compile time to a single target). Added `two_adopters_of_the_same_abstract_class_keep_their_own_override_at_the_same_slot` and `a_class_implementing_an_abstract_class_and_an_interface_dispatches_both`.

## 3. Cross-cutting correctness tests (do not skip)

- [x] 3.1 End-to-end `.zrk` fixture: declare `abstract class Shape { abstract fn area(): Float64; }`, two concrete adopters (`Circle`, `Square`) each with their own `override fn area()`, a function taking `Shape` and calling `.area()` on it, called once with each concrete type — confirm each call dispatches to the correct adopter's own implementation. This is the single most important test in this change, mirroring the existing working `Throwable` case but for user code. Added as `crates/zirk-cli/tests/corpus/valid/abstract_class_dynamic_dispatch.zrk` (`area(): Int32` rather than `Float64`, to keep the corpus's existing float-formatting caveats out of scope — the dispatch mechanism under test does not depend on the return type's width).
- [x] 3.2 End-to-end `.zrk` fixture: a concrete class implementing both a user abstract class and a plain interface, calling a method from each through their respective abstract/interface-typed variables. Added as `crates/zirk-cli/tests/corpus/valid/abstract_class_and_interface.zrk`.
- [x] 3.3 Confirm no regression in the native exception hierarchy's own dynamic dispatch (`catch Throwable(e) { e.message() }`-style tests) — run the existing corpus fixtures that exercise this, not just the new ones. `exceptions_throw_try_catch_finally.zrk`/`.out` and `resources_match_with_closes_on_thrown_exception.zrk`/`.out` still pass unchanged, as do every native-hierarchy unit test in `crates/zirk-sema/tests/typing.rs` (`valid_throw_and_catch_by_concrete_type` and neighbors).
- [x] 3.4 Ran `LLVM_SYS_201_PREFIX=/opt/homebrew/opt/llvm@20 cargo test --workspace` (974 passed, 27 suites); `cargo clippy --workspace --all-targets` clean; `cargo fmt --check` clean.

## 4. Documentation and status sync

- [ ] 4.1 Update `docs/handbook/13-appendices/07-current-limitations.md` and `docs/handbook/11-reference/12-feature-status.md` — drop "abstract-class dynamic dispatch" from the "Several Phase 3 constructs..." bullet/row once delivered.
- [ ] 4.2 Check `docs/handbook` for any existing abstract-class teaching material with a stale "not yet implemented" caveat — reconcile.
- [ ] 4.3 Commit the zirk-lang changes, then run `./scripts/sync-website-content.sh` from the repo root (with `--audit-date YYYY-MM-DD` using the actual date if project-status evidence changed).
- [ ] 4.4 Report both the zirk-lang and zirk-lang-site revisions used.

## 5. OpenSpec close-out

- [ ] 5.1 Run `openspec validate fase-3-abstract-dispatch` before archiving.
- [ ] 5.2 Archive the change once implementation, tests, and documentation sync are complete.
