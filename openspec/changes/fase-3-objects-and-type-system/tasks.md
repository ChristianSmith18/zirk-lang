> **Las secciones son una lista de cobertura, no un orden de ejecución.**
>
> Clases, contratos y genéricos son un núcleo mutuamente dependiente: las clases
> necesitan genéricos para ser útiles, los genéricos necesitan contratos para
> restringir, y los contratos necesitan clases para implementarse. No existe un
> orden en que una parte se termine antes que las otras, así que leer esto como
> "terminar todas las clases antes de empezar contratos" llevaría a un callejón.
>
> La implementación avanza por **cortes verticales**:
>
> 1. Crear las representaciones mínimas compartidas: símbolos nominales,
>    parámetros de tipo, contratos, miembros y relaciones entre tipos.
> 2. Parsear el subconjunto mínimo de clases, interfaces y genéricos.
> 3. Registrar las declaraciones y resolver las referencias entre ellas, aunque
>    sus cuerpos todavía no estén verificados por completo.
> 4. Completar en conjunto la implementación de contratos, las restricciones
>    `from` y los miembros genéricos.
> 5. Añadir lowering y codegen **solo** después de cerrar cada construcción en
>    parser, resolución y chequeo.
> 6. Expandir después hacia traits, herencia, tipos algebraicos, casts y
>    optimizaciones.
>
> Cada caja marcada significa que su regla está cubierta de punta a punta, no
> que su capa esté terminada.

## 1. Léxico

> El cambio `alinear-implementacion-con-norma-refinada` adelantó parte de este
> grupo: `interface` y `trait` ya son palabras clave y ya declaran esta fase.
> Y corrigió el supuesto de 1.1: **`value` no debe ser palabra reservada**.
> Reservarla rompería `mut value = 1;` y `match r { Ok(value) => ... }`, que es
> código Zirk corriente —el propio spec lo usa en sus ejemplos—. `value class`
> se reconoce por posición, igual que `strict` en `inmut::strict`.

- [x] 1.1 Añadir las palabras clave que faltan: `interface` y `trait`, ya hechas por el cambio de alineación. `value` queda contextual, no reservada
- [x] 1.2 Retirar de `phase()` lo que esta fase implementa: `class`, `construct`, `this`, `record`, `type`, `public`, `private`, `protected`, `abstract`, `implements`, `extends`, `from`, `as`, `is`, `interface`, `trait` — `Keyword::phase()` en `zirk-lexer/src/token.rs` ya no gatea `Record | Type | Implements | Abstract | From | As | Interface | Trait` contra `Phase::THREE` (el resto de la lista ya no estaba en el match). Al liberar `abstract`, el parser dejó de rechazarlo con el mecanismo genérico de fase cuando aparece fuera de `abstract class`; encontré y arreglé ese hallazgo con un diagnóstico propio (`codes::ABSTRACT_OUTSIDE_ABSTRACT_CLASS`, E0312) en `parse_class_member`, y actualicé los tests que asumían el gate viejo
- [x] 1.3 Tests: `value class` se parsea sin que `value` deje de ser un identificador válido (`valid_value_does_not_stop_being_an_ordinary_identifier`), y las palabras retiradas dejan de declarar fase (`implemented_keywords_declare_no_phase` en `zirk-lexer/src/token.rs`)
- [x] 1.4 Al implementar genéricos, partir el token `>>` en dos `>` dentro del parser de tipos: `Box<Box<Int32>>` termina en el token de desplazamiento

## 2. Gramática — clases y contratos

- [x] 2.1 Parsear `class` con campos, métodos, campos `public mut` por defecto y múltiples `construct`
- [x] 2.2 Parsear los modificadores de visibilidad y `abstract` sobre clases y miembros
- [x] 2.3 Parsear `extends` con una sola clase. `implements` llega con los contratos
- [x] 2.4 Parsear `this` como expresión, rechazándolo fuera de una clase
- [x] 2.5 Parsear `interface`, rechazando cuerpos en sus métodos
- [x] 2.6 Parsear `trait`, admitiendo cuerpos
- [x] 2.7 Tests: un caso válido y uno inválido por cada regla nueva

## 3. Gramática — genéricos

- [x] 3.1 Parsear parámetros de tipo `<T>` en funciones, clases y tipos de datos
- [x] 3.2 Parsear restricciones con `from`
- [x] 3.3 Parsear argumentos de tipo en los usos (`Box<Int32>`)
- [x] 3.4 Distinguir `<` de apertura de genéricos y de comparación — sin ambigüedad: los genéricos solo se parsean en posición de tipo, nunca en expresiones, así que `<` de comparación no se toca
- [x] 3.5 Tests: un caso válido y uno inválido por cada regla nueva

## 4. Gramática — tipos de datos y casts

- [x] 4.1 Parsear variantes de enum con datos asociados — `Name(nombre: Type, ...)` con campos nombrados (no solo tipos posicionales, corregido tras revisar el handbook), y el mapping tradicional `Name -> value`; `EnumDecl.variants` pasa a `Vec<EnumVariant>`, con `AssociatedField` nombrado
- [x] 4.2 Parsear patrones con destructuring de variantes — `EnumName.Variant(binding, ...)`, con calificación de enum obligatoria en todos los casos (decisión explícita: el spec muestra patrones sin calificar para algebraicos, pero se prioriza consistencia con lo ya implementado para tradicionales)
- [x] 4.3 Parsear `record` y value classes — `ClassDecl` gana `kind: ClassKind` (Class/Record/ValueClass) y se reutiliza toda la infraestructura de clases; `value` es contextual, solo especial delante de `class` (posición, igual que `strict` en `inmut::strict`)
- [x] 4.4 Parsear uniones `A | B` y alias con `type` — `TypeRef` gana `union_with: Vec<TypeRef>`; `parse_type` (con `|`) queda separado de `parse_type_atom` (sin `|`), usado en argumentos genéricos y restricciones `from` a propósito para no expandir el alcance
- [x] 4.5 Parsear casts postfijos (`as`) y prefijos (`<T>`) — `Expr::Cast`; postfix va en la cadena de `parse_member_chain`, prefix en `parse_primary` reusando `eat_gt` para cerrar `<...>`. Extraje ambos a funciones propias: `parse_primary` corre una vez por nivel de anidación, y el arm nuevo casi agota el stack de 2 MB del test de límite de anidación en build debug — encontrado y arreglado antes de cerrar
- [x] 4.6 Rechazar los casts que exigen `unsafe`, indicando la fase que los trae — no hay nada que rechazar todavía: el único caso mencionado (`Pointer<T>`, dereferencia cruda) no tiene tipo `Pointer` implementado, así que ya cae en "tipo pendiente" antes de llegar a un cast
- [x] 4.7 Parsear `?.`, retirando el diagnóstico de fase de la fase anterior — la gramática ya lo aceptaba (`QuestionDot`); el checker lo rechazaba siempre con un diagnóstico de "no implementado", sin condición — no era 5.9 de verdad, esa casilla estaba mal marcada. Corregido junto con 5.9
- [x] 4.8 Rechazar temporalmente `Function(...) => R` y `Fn(...) => R` en posición de tipo, indicando la fase que los implementa (D9) — `reject_function_type` reconoce `Fn`/`Function` seguido de `(` en `parse_type_atom`, consume la forma completa para recuperación y reporta `NOT_IMPLEMENTED` en vez de dejar que el `(` confunda al parser de parámetros. D9 no fija un número de fase futura, así que el mensaje no inventa uno
- [x] 4.9 Tests: un caso válido y uno inválido por cada regla nueva — cubierto en `crates/zirk-parser/tests/grammar.rs` para 4.1–4.8 (uniones, aliases, records, value classes, casts, `?.`, tipos función rechazados)

> **El spec de objetos se actualizó el 17 de agosto de 2026 y varias reglas de
> este grupo cambiaron de contenido.** Lo ya implementado se realineó:
>
> - Todo atributo omitido recibe el default de su tipo antes del constructor, así
>   que este solo debe escribir lo que no tiene default (una clase no lo tiene).
> - `super(...)` y `super.method()` existen, lo que retira la regla provisional
>   de que una subclase inicializara los campos heredados.
> - Reemplazar un método heredado exige `override fn`.
> - `abstract class` dejó de ser algo que se extiende: es un conjunto de
>   requisitos que se adopta con `implements`, sin constructor ni layout.

## 5. Tipos — nominalidad y miembros

- [x] 5.1 Representar clases, records, value classes y enums como tipos nominales
- [x] 5.2 Construir la jerarquía de herencia y detectar ciclos
- [x] 5.3 Resolver miembros contra el tipo y su cadena de herencia
- [x] 5.4 Verificar visibilidad, distinguiendo miembro oculto de miembro inexistente
- [x] 5.5 Admitir subclase donde se espera la base, y rechazar la dirección contraria
- [x] 5.6 Verificar `construct`: que exista, resolver por aridad e inicializar todo campo. Falta desempatar firmas de igual aridad por tipo y por etiqueta, que llega con los argumentos nombrados reordenados
- [x] 5.7 Verificar la redefinición de métodos: misma firma, y rechazo si difiere — llega con `extends`, que es lo que hace posible redefinir
- [x] 5.8 Verificar `abstract class` como conjunto de requisitos adoptado con `implements`: sin constructor, sin estado y sin contribución al layout — `ClassKind::Abstract` reutiliza toda la infraestructura de clases; rechaza `extends` y `construct` (igual que record/value class), miembros son `abstract fn` sin cuerpo. `implements` ahora resuelve contra contratos O clases abstractas (`ClassType.abstract_bases`, tabla separada de `contracts` porque son espacios de ids distintos); a diferencia de un contrato, satisfacer un requisito exige `override fn` explícito. Tuve que relajar el chequeo existente de "override no sobreescribe nada" cuando la clase tiene `implements`, porque ese chequeo corre antes de que la conformidad con clases abstractas se resuelva. Bloqueado por `NOT_LOWERED`: un valor tipado a través de una clase abstracta necesitaría despacho dinámico que no existe para este camino
- [x] 5.9 Tipar `?.` como el tipo del miembro en forma nulable (D7) — la casilla estaba marcada pero `check_field`/`check_call` rechazaban `expr.safe` sin condición (`self.not_checked`); ahora `member_type`/`check_method_call_on` narrowean el receptor y envuelven el resultado en nullable, tanto para campo como para método. Encontré y arreglé otro crash real de lowering en el camino; bloqueado por `NOT_LOWERED` hasta 10.8
- [x] 5.10 Rechazar anotar una closure como parámetro, retorno o campo (D9) — consecuencia directa de 4.8: sin sintaxis para escribir el tipo de una closure, ningún parámetro/retorno/campo puede declararla, así que cualquier intento cae en un `TYPE_MISMATCH` ordinario
- [x] 5.11 Rechazar que una closure escape de la función que la crea (D9) — mismo mecanismo para `return`; encontré y cerré un resquicio real: inferir un parámetro genérico `T` desde un argumento closure sí colaba sin 4.8 de por medio, así que `infer_type_params` lo rechaza explícitamente ahora
- [x] 5.12 Tests: una closure sigue funcionando en variable local e invocación (D9) — al verificarlo encontré y arreglé un crash real y preexistente: una closure que lee `this` dentro de un método (`n + this.value`) no lo registraba como captura (`Expr::This` no pasa por `Scopes::resolve`), así que el lowering de IR pedía un slot `this` que nunca se declaró. Agregado `record_this_capture`; test de corpus en `crates/zirk-cli/tests/corpus/valid/closure_captures_this.zrk`
- [x] 5.13 Tests: un caso válido y uno inválido por cada regla nueva — auditado contra `crates/zirk-sema/tests/typing.rs`: cada regla de 5.1–5.12 y 5.15 ya tiene su par al haberse escrito junto con la tarea que la introdujo (nominalidad, jerarquía y ciclos, miembro oculto vs inexistente, sustitución de subclase, `construct` por aridad, `override` con firma distinta/sin la palabra/que no sobreescribe nada, `abstract class` completo y con hueco, `?.`, anotación y escape de closure, captura de `this`, matriz de aliasing estricto). Sin huecos reales que llenar; no se agregaron tests de relleno
- [ ] 5.14 Rechazar shadowing ordinario y resolver una captura homónima únicamente mediante `this.nombre` — intenté implementarlo (rechazar que un parámetro de lambda comparta nombre con una variable capturable) y rompió `crates/zirk-cli/tests/corpus/valid/capture_by_value.zrk`, que ya documenta y prueba lo contrario: "Shadowing resolves the same way it does outside a closure". Revertido. **Contradicción real entre `design.md` (D10: "no hay shadowing ordinario") y el corpus ya validado** — queda para que el equipo decida cuál es la regla vigente antes de tocar esto de nuevo
- [x] 5.15 Aplicar la matriz `mut`/`inmut`/`inmut::strict` a referencias de objeto y rechazar aliases que rompan strictness (D11) — `inmut::strict` ya se parsea (`Mutability::Strict`, reconocido por posición tras `inmut::` igual que `strict`/`value` en otros puntos de la gramática); reasignar una `inmut::strict` se rechaza igual que `inmut` (`require_writable`). Alcance: D11 delega la estrategia concreta de análisis y memoria a las fases correspondientes, así que esto cubre la regla al nivel de ligadura de variable para objetos/contratos de esta fase (`is_reference_type`: `class` ordinaria o contrato, no records/value classes que son inline por 11.5) — "solo mover la variable completa comparte referencia" (D11), así que la única forma detectada es `let` inicializado con un `Expr::Path` directo a otra variable. Dos violaciones nuevas con `codes::STRICT_ALIAS_VIOLATION` (E0430): una `strict` no puede volverse alias mutable, y no puede adquirirse desde un alias `mut` todavía accesible. La semántica de copia profunda en proyecciones (campo/índice/slice) y el contrato `Clone`/`Cloneable` que D11 también menciona quedan fuera: son una superficie propia (aparecen en `CORE_LANGUAGE_SEMANTICS.md`, `MEMORY_AND_UNSAFE_SEMANTICS.md`, etc.) que ninguna otra tarea de esta fase registra como propia

## 6. Tipos — contratos

- [x] 6.1 Registrar interfaces y traits como contratos, con sus métodos
- [x] 6.2 Verificar que una declaración implemente por completo lo que dice implementar
- [x] 6.3 Copiar en la clase los métodos de trait con cuerpo que no redefine (D4)
- [x] 6.4 Rechazar el conflicto entre dos traits que aportan el mismo método (D4) — dos defaults distintos para el mismo nombre es `DUPLICATE_DECLARATION`; si la propia clase escribe el método, no hay conflicto (ella decidió)
- [x] 6.5 Admitir una implementación donde se espera su contrato
- [x] 6.6 Definir los contratos de operador y sus métodos reservados (`_add`, `_subtract`, etc.), impidiendo reabrir tipos nativos (D6)
- [x] 6.7 Resolver los operadores por contrato en vez de por lista fija de tipos
- [x] 6.8 Hacer que `String` implemente el contrato de concatenación, cerrando la deuda de `+`
- [x] 6.8a Hacer que `String` implemente repetición checked en ambos órdenes (`String * Integer`, `Integer * String`)
- [x] 6.9 Definir `Iterable<T>` e `Iterator<T>` como contratos del lenguaje — implicó generalizar genéricos a contratos y enums (antes solo clases): `ContractDecl`/`EnumDecl` ganan `type_params`, `implements` pasa de `Vec<Ident>` a `Vec<TypeRef>` para admitir `implements Iterable<Int32>`, y se suman `Base::ContractInstance`/`Base::EnumInstance` con sus tablas de interning (mismo patrón que `Base::Instance` para clases genéricas). `Iterable<T>`, `Iterator<T>` y el enum `Iteration<T> { Item(T), Done }` se inyectan directo en las tablas del checker (`register_native_iteration_contracts`), no vía el parser: así ningún programa paga el `NOT_LOWERED` de una declaración genérica de usuario solo por existir. Nombres reservados: reabrir `Iterable`/`Iterator`/`Iteration` se rechaza igual que reabrir un tipo nativo. Encontré y arreglé dos hallazgos reales al verificar con un programa completo: `check_return` comparaba con `Type::accepts` solo, sin `is_subclass_of`, así que retornar una subclase o un implementador de contrato desde una función ya estaba roto antes de esta tarea — corregido; y la construcción de variante de un enum genérico (`Iteration.Done`) no tiene sintaxis para inferir `T`, así que agregué `bare_enum_matches_instance` como aproximación sensata (la variante desnuda se acepta donde se espera su instancia) — sound porque nada corriente lee ese argumento: construir una instancia genérica ya cae en `NOT_LOWERED` antes de que importe
- [x] 6.10 Hacer que `for ... in` exija `Iterable<T>`, y que rangos y `String` lo implementen (D8) — `element_type` ahora resuelve `T` buscando en `ClassType.contract_instances` de la clase iterada (nueva tabla, paralela a `contracts`, que registra la instanciación concreta que satisface, no solo el contrato base); una clase que no implementa `Iterable<T>` cae en `NOT_ITERABLE` con el mensaje actualizado. Los rangos siguen resolviendo nativamente sin pasar por `implements` — cero cambios de comportamiento, el corpus de Fase 2 sigue verde sin tocarlo. `String` sigue diferido con el mismo `PENDING_FEATURE` de siempre (necesita `Char`, Fase 3b), ahora con la nota de que no puede implementar `Iterable<Char>` hasta que exista. Iterar sobre un tipo propio que sí implementa `Iterable<T>` tipa correctamente (incluye `T`, con dispatch verificado hasta el mismo punto que cualquier contrato) pero el `for` en sí cae en `NOT_LOWERED`: despachar por la tabla del contrato (10.7) y hacer match sobre `Iteration<T>` con datos asociados (11.3/11.4) no bajan todavía
- [x] 6.11 Tests: un caso válido y uno inválido por cada regla nueva

## 7. Tipos — genéricos

- [x] 7.1 Representar parámetros de tipo y su alcance en la declaración — `Base::Param`, con un id estable por `TypeParam` (por su span) para que declarar y luego chequear el cuerpo resuelvan el mismo `T`. Reportado con `NOT_LOWERED` hasta que 7.2–7.6 cierren: se resuelve el nombre y se chequea el cuerpo, pero no se baja a IR
- [x] 7.2 Verificar el cuerpo genérico una sola vez contra sus restricciones (D5) — sin monomorfización el cuerpo solo existe una vez, y 7.4 es lo que lo verifica contra `from`
- [x] 7.3 Verificar en el sitio de uso que el argumento cumple la restricción `from` — `resolve_type` instancia `Box<Int32>` de verdad (`Base::Instance`, interning por `(class, args)` igual que `Base::Function`), valida aridad (`WRONG_ARGUMENT_COUNT`) y cada argumento contra las constraints del parámetro (`TYPE_MISMATCH`). Sigue bloqueado por `NOT_LOWERED` en la propia declaración de la clase genérica (7.1), así que ningún programa que use esto compila todavía — pero el chequeo del sitio de uso ya es real
- [x] 7.4 Rechazar en el cuerpo lo que la restricción no garantiza — acceder a un campo o llamar a un método a través de `T` se resuelve contra las constraints de `T` (clases y contratos); lo que ninguna promete es `UNKNOWN_MEMBER`, y sin constraints no hay miembros
- [x] 7.5 Rechazar varianza, tipos asociados y orden superior con diagnóstico propio — `in`/`out` se parsean y se rechazan con un diagnóstico dedicado (`PENDING_FEATURE`, distinto del `NOT_LOWERED` genérico de 7.1); tipos asociados y orden superior no tienen sintaxis en el lenguaje, así que no hay nada que rechazar todavía
- [x] 7.6 Unificar tipos genéricos en llamadas, inferiendo el argumento cuando es inequívoco — solo desde los argumentos de una función (no de receptor, resultado esperado ni contexto, y no en métodos/constructores todavía); conflicto entre dos argumentos para el mismo `T`, `T` no determinado por ningún argumento, y el tipo inferido que no cumple su `from` son errores explícitos en vez de una solución arbitraria, tal como pide el spec
- [x] 7.7 Tests: un caso válido y uno inválido por cada regla nueva — cubierto en `crates/zirk-parser/tests/grammar.rs` (3.1–3.5) y `crates/zirk-sema/tests/typing.rs` (7.1–7.6)

## 8. Tipos — tipos de datos y casts

- [x] 8.1 Extender los enums con datos asociados, conservando los tradicionales, su nombre como valor por defecto y mappings explícitos con `->` — `EnumType.variants: Vec<EnumVariantInfo>` con campos asociados tipados; `->` valida que el valor sea `String` o `Int32`. Bloqueado por `NOT_LOWERED` en la propia declaración de la variante con datos: `Base::Enum` sigue bajando a un discriminante plano (11.3/11.4 lo resuelven)
- [x] 8.2 Verificar aridad y tipos de un constructor de variante — `EnumName.Variant(args)` se despacha como una `Signature` sintética por `check_direct_call`, igual que un constructor de clase: aridad, nombres y tipos siguen la misma regla que cualquier llamada
- [x] 8.3 Tipar los patrones con destructuring, ligando los nombres a su tipo — `check_pattern` acumula bindings (antes solo el caso `Pattern::Binding` de nivel superior estaba especial-casado) y `check_match` los declara todos juntos en el scope del brazo, después del `push()`
- [x] 8.4 Extender la exhaustividad a enums con datos asociados — sin cambios necesarios: la cobertura ya se rastreaba por nombre de variante, independiente de si carga datos
- [x] 8.5 Implementar records: inmutables, con igualdad estructural derivada — construcción implícita solo por nombre (campos omitidos toman el default de su tipo), sin `extends` ni `construct` propio (rechazados con diagnóstico), campos siempre inmutables (la mutación desde un método se rechaza gratis: sin `construct`, `in_constructor` nunca es true), `==`/`!=` legal sin `_equals`. Bloqueado por `NOT_LOWERED`: no hay `construct` que bajar ni codegen de igualdad derivada (11.5)
- [x] 8.6 Implementar value classes: sin identidad observable — `value class Name(campo: Tipo, ...);` se desazucara a la misma forma que un record; `is` se rechaza igual que en records (ninguno de los dos tiene identidad)
- [x] 8.7 Implementar uniones y exigir discriminarlas antes de usarlas — `Base::Union(u32)` interna la lista normalizada (orden irrelevante, duplicados fuera, subsunción por herencia); `T | Null` se pliega al bit `nullable` de `Type` en vez de ser una `Union` de verdad. Discriminar: acceder a un miembro antes de narrowear se rechaza siempre (`UNKNOWN_MEMBER`) — el narrowing por patrón de tipo en `match` no está implementado, así que por ahora "antes de narrowear" es "siempre". Bloqueado por `NOT_LOWERED`: IR no representa uniones (11.x)
- [x] 8.8 Implementar alias con `type` — transparente: `resolve_type` resuelve el target en lugar de crear una variante de `Base` propia, así que no necesita `NOT_LOWERED` por representación... salvo que el lowering de IR vuelve a resolver nombres de tipo desde el AST en algunos caminos (`ir_type_from_ref`) y no conoce alias todavía, así que sigue bloqueado por eso — encontré y arreglé otro crash real antes de cerrarlo
- [x] 8.9 Verificar casts: admitir los relacionados, rechazar los que no lo están — admite identidad, clase↔base/contrato (en cualquier dirección, comprobado en runtime cuando llegue lowering), y miembro de una unión; rechaza el resto (`String as Int32`, clases no relacionadas) en tiempo de compilación. Bloqueado por `NOT_LOWERED`: no hay verificación en runtime todavía (11.6)
- [x] 8.10 Tests: un caso válido y uno inválido por cada regla nueva — cubierto en `crates/zirk-sema/tests/typing.rs` para 8.1–8.9

## 9. Runtime — alocación de objetos

- [x] 9.1 Añadir la función de alocación como `extern "C"` tras la frontera ABI (D1, ADR-002)
- [x] 9.2 Documentar en el runtime que no libera y que la estrategia llega en Fase 4
- [x] 9.3 Tests: el símbolo aparece sin mangling en la biblioteca estática
- [x] 9.4 Tests: un programa que construye objetos termina con código de salida cero

## 10. IR — objetos y despacho

- [x] 10.1 Representar el layout de un objeto: cabecera y campos (D2)
- [x] 10.2 Colocar los campos heredados antes que los propios (D2)
- [x] 10.3 Bajar la construcción a alocación abstracta más inicialización
- [x] 10.4 Bajar el acceso a campo a lectura por desplazamiento
- [x] 10.5 Bajar la llamada directa cuando el método no es redefinible (D3). Sin herencia ninguno lo es, así que hoy toda llamada es directa
- [x] 10.6 Bajar la llamada indirecta por tabla cuando lo es (D3)
- [x] 10.7 Bajar el despacho a través de un contrato por la tabla de la interfaz (D3) — encontrado ya implementado al auditar (no estaba marcado): `ObjectLayout.contracts: Vec<ContractTable>` en `zirk-ir/src/ir.rs`, poblado en `lower.rs` con una tabla por contrato satisfecho en el orden propio del contrato; `lower_contract_call` emite `InstKind::CallContract` cuando el receptor se tipa por contrato. Verificado end-to-end con un programa real (`interface Greeter` + dos clases + una función que recibe `Greeter` y despacha `.greet()`) y con el corpus ya existente `contracts.zrk` (que ya ejercía esto sin que la casilla lo reflejara)
- [x] 10.8 Bajar `?.` a comprobación de nulidad con dos bloques (D7) — hecho para acceso a campo (`lower_safe_field`, ya cerrado antes) y ahora también para llamada a método (`u?.greet()`): `lower_safe_method_call` en `zirk-ir/src/lower.rs` reusa el mismo patrón de dos bloques ausente/presente con casilla resultado que `lower_safe_field`, pero con la llamada misma dentro del bloque presente — directa, por tabla propia (`CallVirtual`) o por tabla de contrato (`CallContract`), resuelto sobre el receptor ya desenvuelto, según si el receptor detrás de la nulabilidad es `IrType::Object` o `IrType::Contract` (`safe_method_call_info`, compartida con `type_of` para que el tipo de la llamada se conozca antes de bajarla). El gate `NOT_LOWERED` en `check_call` se relajó en el checker para dejar pasar receptor de clase, instancia genérica o contrato; se mantiene solo para un receptor por parámetro genérico, que no tiene cuerpo concreto al que llamar todavía.
  Encontré y arreglé tres hallazgos reales al desbloquear este camino: `is_closure_call` pedía el tipo del callee como si `u?.greet` fuera un campo ordinario (vía `type_of`'s rama `Field`), crasheando porque "greet" no es un campo de `User` — le faltaba el mismo guard que ya tenía para `method_of`/`contract_method_of`; `lower_expr_for_effect` (una llamada usada como sentencia, `u?.greet();`) caía en la rama de llamada por nombre libre y pedía `signature_return("greet")`, que tampoco existe como función — necesitaba su propia rama, igual que la tienen la llamada a método y a contrato; y `lower_expr_as` sólo envolvía con `Wrap` al ensanchar `T → T?`, sin retipar primero cuando el `T` de origen y el de destino difieren (p. ej. `mut s: Speaker? = User("leo")`, clase a contrato) — el `Wrap` recibía un operando `Object` donde el verificador esperaba `Contract`; agregado el mismo retipado que la rama no nulable ya hacía, ahora también en la rama nulable.
  Verificado con un programa real compilado y corrido (`crates/zirk-cli/tests/corpus/valid/safe_method_call.zrk`): receptor de clase presente y ausente, receptor de contrato presente y ausente (con el retipado clase→contrato de por medio), y un método que ya retorna `String?` (sin doble envoltura). Nuevo test en `crates/zirk-sema/tests/typing.rs` cubre tanto el caso aceptado (antes `invalid_safe_method_call_is_not_lowered_yet`, ahora `valid_safe_method_call_lowers`) como el gate que sigue vigente para un receptor por parámetro genérico (`invalid_safe_method_call_through_generic_param_is_not_lowered`)
- [x] 10.9 Extender el verificador a las instrucciones nuevas
- [x] 10.10 Tests: IR esperada para cada construcción nueva

## 11. IR — genéricos y tipos de datos

- [x] 11.1 Especializar cada combinación de argumentos de tipo usada (D5) — al investigar encontré y corregí un hallazgo real y previo a esta tarea, que bloqueaba cualquier uso práctico de una clase genérica incluso antes de pensar en IR: `check_construction` armaba la `Signature` del constructor con `type_params: Vec::new()`, así que `Box(5)` nunca inferían `T` y el resultado tipaba como `Base::Class` liso en vez de `Base::Instance` — `mut b: Box<Int32> = Box(5)` ya fallaba con `TYPE_MISMATCH` antes de llegar siquiera al `NOT_LOWERED`. Corregido: la construcción ahora infiere los argumentos de tipo (reutilizando `infer_type_params`/`check_direct_call_with_subst`, la variante de 7.6) y tipa como `Base::Instance` de verdad; `member_type`/`check_method_call_on` ganan el caso `Base::Instance`.
  Alcance del checker: `check_generic_class_lowering` (corre después de `declare_members`, para tener campos/métodos resueltos) gatea con `NOT_LOWERED` solo lo que la especialización directa no cubre — `extends`, `implements`, o `T` anidado dentro de otro tipo genérico (`Box<T>`, `T | Int32`) en vez de nombrado directo (`type_references_any_param`) — una clase genérica que solo usa su `T` de forma directa en campos/constructor/métodos ya no dispara `NOT_LOWERED` en absoluto.
  IR: `zirk-ir/src/lower.rs` clona el `CheckedProgram` y le agrega, por cada entrada de `checked.generic_instances`, un `ClassType` especializado (`specialize_class`: sustitución de un solo nivel, sin recursión, porque el gate del checker ya garantiza que no hay nesting) bajo un nombre propio (`Box$0`); la clase genérica original queda con un `ObjectLayout` vacío y su cuerpo nunca se baja — cada especialización baja el mismo AST de nuevo bajo su propio id. `Base::Instance(id)` mapea a `instance_base + id` (`instance_base = checked.classes.len()` antes de anexar). Encontré y arreglé tres hallazgos reales en el camino: `run_method` leía el tipo de retorno desde la anotación escrita (`T`, sin sustituir) en vez del `ClassType` ya sustituido — panicaba con "a verified program only names known types"; los símbolos de constructor/método usaban el nombre AST (`class.name.name`, siempre "Box") en vez del nombre de la clase chequeada (`Box$0`), lo que habría colisionado el símbolo de dos instanciaciones distintas; y una construcción (`Box(5)`) se resolvía por nombre desnudo (`class_id`), incapaz de distinguir `Box<Int32>` de `Box<String>` — agregado `CheckedProgram.generic_constructions: HashMap<Span, u32>`, que el checker llena en el sitio de construcción y que `construction_class_id` consulta primero. Verificado con programas reales compilados y corriendo: construcción/método/campo genérico simple, dos instanciaciones distintas coexistiendo sin colisión, reutilización de la misma instanciación desde tres sitios distintos, y un genérico con `from` sobre una jerarquía (`Cage<T from Animal>` construido con `Dog`, despachando el método redefinido de verdad)
- [x] 11.2 Reutilizar la especialización cuando la combinación se repite — gratis por construcción: `checked.generic_instances` interna por `(class, args)`, así que toda instanciación repetida ya resuelve al mismo id antes de que la especialización de 11.1 la vea; verificado con un programa real que construye `Box<Int32>` desde tres sitios distintos (`generic_class_specialization.zrk`)
- [x] 11.3 Bajar los enums con datos asociados: discriminante más carga útil — `IrType::Enum(u32)` nuevo, con tabla propia `EnumLayout` en el id space de `checked.enums`. La "carga útil" no es un union real a nivel de bytes: cada variante recibe su propio tramo de campos, concatenados (no superpuestos) — más simple y sin riesgo de reinterpretar los bytes de otra variante, al costo de un layout dimensionado para todas las variantes a la vez en vez de solo la más grande; con el número de variantes típico de esta fase el costo no aprieta. Un enum tradicional (ninguna variante con datos) sigue siendo `Int32` liso, sin cambio de representación ni de rendimiento. Construcción propia (`InstKind::BuildEnum`, sin `Alloc`) y lectura del discriminante (`InstKind::Discriminant`, `extractvalue` en LLVM). Encontré y arreglé tres hallazgos reales al desbloquear este camino, todos preexistentes y nunca antes alcanzados porque la construcción de variantes con datos estaba completamente bloqueada: `contract_method_of` y `lower_contract_call` no comprobaban `variant_accesses` antes de pedir el tipo del receptor (a diferencia de `method_of`/`lower_method_call`, que sí lo hacían) — `EnumName.Variant(...)` como callee de una llamada crasheaba al intentar resolver "EnumName" como variable; y `arm_value_type` pedía el tipo del cuerpo del primer brazo de un `match` antes de que el bucle real declarara los bindings de ese brazo, así que cualquier `match` cuyo primer brazo leyera su propio binding (`y => y`, no solo el nuevo caso de destructuring) crasheaba — nunca se había notado porque ningún test previo escribía ese patrón exacto
- [x] 11.4 Bajar el destructuring de patrones a lecturas de la carga útil — reutiliza `InstKind::LoadField` sin instrucción nueva: cada sub-patrón de un `Variant` que sea `Binding` se liga a su campo vía `LoadField` con el índice aplanado que `EnumLayout.variants[discriminante]` ya da. Alcance: el checker exige que cada sub-patrón sea `Binding` o `Wildcard` (`codes::NOT_LOWERED` en `check_variant_pattern` para cualquier otra forma) — un patrón literal o un `Variant` anidado dentro de una destructuración necesitaría compilar una condición combinada que esta pasada no construye. También gateado: un enum genérico con datos (combina con la especialización de 11.1, fuera de alcance, igual que records/value classes en 11.5). Verificado con programas reales: construcción con argumentos nombrados reordenados, `match` con bindings usados en el cuerpo, wildcard descartando un campo, y los dos gates (genérico, patrón literal anidado) fallando limpio sin crash
- [x] 11.5 Bajar records y value classes sin indirección — `IrType::Value(u32)` nuevo en `zirk-ir/src/ir.rs`, con su propia tabla `ValueLayout` (solo campos, sin cabecera ni tabla de despacho) e id compartido con `ObjectLayout`/`checked.classes` (`ClassType.kind` decide cuál tabla mirar). Construcción propia (`InstKind::BuildValue`, empaqueta campos sin `Alloc`, con relleno de defaults para campos omitidos por nombre); lectura de campo reutiliza `LoadField` sin cambios en `lower.rs` — solo el backend LLVM distingue: `extractvalue` sobre el valor cuando es `IrType::Value`, GEP+load sobre el puntero cuando es `IrType::Object`. En LLVM, `llvm_type_in` ahora devuelve el struct mismo (no un puntero) para un valor, así que se pasa por valor en slots, parámetros, retorno y como campo de otro objeto o valor — sin cambio de convención de llamada especial, LLVM ya soporta agregados por valor. Alcance: solo record/value class no genéricos (combinar con la especialización de 11.1 queda fuera, gateado en el mismo punto que antes); igualdad estructural (`==`/`!=`) type-checka sin `_equals` pero su lowering queda gateado con `NOT_LOWERED` propio — la semántica existe (el spec la deriva de cada campo) pero recorrer estructuralmente, incluyendo el caso de un campo que sea otro record anidado, es una pieza separada de "sin indirección" en sí. Verificado con programas reales: construcción/lectura simple, value class pasada y retornada por función, record anidado dentro de otro record, default de campo omitido, y `Point?` con navegación segura `?.` — todos correctos end-to-end
- [x] 11.6 Bajar el cast comprobado a comparación de descriptor — el descriptor gana una segunda lista, además de la de contratos: los ids de la clase misma y de cada base transitiva (`ObjectLayout.ancestors`), que un cast comprobado busca en tiempo de ejecución. `InstKind::CheckedCast` carga el descriptor del objeto y llama a `zirk_rt_check_cast` (nueva, en `zirk-runtime`), que recorre esa lista y aborta el proceso vía `zirk_rt_invalid_cast` si el tipo destino no aparece — no hay `try`/`catch` todavía (Fase 4), así que un cast fallido es terminación controlada, igual que cualquier otro invariante que este módulo ya vigila (overflow, división por cero, contrato faltante). `zirk_rt_contract_table` se actualizó para la nueva forma del descriptor `[tabla_métodos | cantidad_ancestros | ids... | cantidad_contratos | pares...]`. Alcance: el checker (`cast_is_directly_lowerable`) solo baja el cast identidad (mismo tipo, cualquier clase — sin chequeo, la clase base ya cumple `Object`/`Value`/`Enum` por igual) y clase↔clase o contrato→clase, ambos no nulos; unión (no baja en absoluto todavía, 8.7/11.x) y restricción de parámetro genérico quedan con `NOT_LOWERED` propio, igual que cualquier cast con alguno de los dos lados nulable (decidir qué significa `null` para el chequeo es una pregunta aparte). Encontré y arreglé un hallazgo real y **no relacionado con casts**, expuesto por el mismo camino: `lower_expr_as` solo sabía ensanchar `T` a `T?`; cualquier ensanchamiento ordinario de objeto — `mut a: Animal = Dog();`, con anotación explícita, o pasar una subclase donde se espera la base como argumento — se lowereaba con el tipo IR del valor concreto (`Object(Dog)`) en vez del esperado (`Object(Animal)`), y el verificador lo rechazaba como IR inválida. Nunca antes se había topado con un test porque ningún programa de la fase escribía ese patrón exacto con anotación explícita. Corregido con `InstKind::Retype`, un cambio de tipo sin trabajo en runtime (la dirección del objeto no cambia, solo qué tipo estático promete). Verificado con programas reales: upcast/downcast entre clase y base, cast contrato→clase (éxito y fallo con exit code 70), identidad sobre un escalar, y los dos gates (unión — pendiente de 8.7/11.x —, nulable) fallando limpio
- [x] 11.7 Tests: IR esperada para cada construcción nueva — agregado a `crates/zirk-ir/tests/lowering.rs`, siguiendo el patrón ya establecido (`compile`/`instructions`) para cada tarea de 10.7 a 11.6: despacho por contrato (`CallContract`), especialización genérica (copia propia por instanciación, reutilizada cuando la combinación se repite, distinta cuando no), records (`ValueLayout` en vez de `ObjectLayout`, `BuildValue` sin `Alloc`, lectura de campo compartida con objetos), enums algebraicos (`EnumLayout` con la carga aplanada y sus índices por variante, `BuildEnum`, `Discriminant` sobre el patrón, lectura de campo por índice aplanado), y casts comprobados (identidad sin `CheckedCast`, downcast con el `target_class` correcto, y el ensanchamiento ordinario vía `Retype` sin cheque). Encontré un detalle real al escribirlos: el contrato/enum nativo `Iterable`/`Iterator`/`Iteration` (task 6.9) ocupa el id 0 de sus tablas por registrarse antes que cualquier declaración de usuario, así que un test no puede asumir índice 0 para el primer tipo que el propio programa declara — hay que buscarlo por nombre

## 12. Backend LLVM

- [x] 12.1 Traducir el layout de objetos, con el prefijo compartido entre base y subclase
- [x] 12.2 Emitir el descriptor de tipo de cada tipo con identidad
- [x] 12.3 Emitir la tabla de métodos por tipo, con índices estables al heredar
- [x] 12.4 Emitir una tabla por interfaz implementada y su búsqueda en el descriptor — igual que 10.7, ya implementado: el descriptor lleva la cuenta de contratos y cada tabla (`crates/zirk-codegen-llvm/src/emit.rs`, alrededor de la emisión del descriptor), y `InstKind::CallContract` se traduce cargando el descriptor y buscando la tabla del contrato en tiempo de ejecución vía `zirk_rt_contract_table` (`crates/zirk-runtime/src/memory.rs`)
- [x] 12.5 Traducir la llamada indirecta del despacho dinámico
- [x] 12.6 Traducir el cast comprobado, transfiriendo al runtime cuando falla — ya implementado junto con 11.6: `InstKind::CheckedCast` traduce a cargar el descriptor y llamar a `zirk_rt_check_cast` (`crates/zirk-codegen-llvm/src/emit.rs`), que aborta el proceso vía `zirk_rt_invalid_cast` si el chequeo falla (`crates/zirk-runtime/src/memory.rs`, `failure.rs`)
- [x] 12.7 Traducir value classes inline, sin puntero intermedio — ya implementado junto con 11.5: `llvm_type_in` devuelve el struct mismo (no un puntero) para `IrType::Value`, así que se pasa por valor en slots, parámetros, retorno y como campo de otro objeto o valor, sin convención de llamada especial — LLVM ya soporta agregados por valor
- [x] 12.8 Tests: el módulo LLVM generado verifica para cada construcción nueva — cubierto indirectamente: el corpus de `zirk-cli` (`the_valid_corpus_compiles_and_produces_the_expected_output`) compila cada construcción nueva hasta LLVM de verdad y ejecuta el binario resultante, comparando la salida — records/value classes (`inline_values.zrk`), enums algebraicos (`algebraic_enums.zrk`), genéricos (`generic_class_specialization.zrk`), Iterable/Iterator (vía `for`), y casts comprobados (`checked_casts.zrk`, éxito y — verificado a mano, no en el corpus — fallo con exit code 70) ya se ejercitan de punta a punta en cada corrida de test

## 13. Verificación de punta a punta

- [x] 13.1 Ampliar el corpus con programas válidos: clases, herencia, contratos, genéricos, tipos de datos, casts — auditado contra `crates/zirk-cli/tests/corpus/valid/`: ya cubre, uno por uno y con LLVM real, cada construcción de esta fase (clases y herencia con `super`/`override`/miembros `protected`/`private` en `inheritance.zrk`, contratos e interfaces/traits con despacho en `contracts.zrk`, genéricos en `generic_class_specialization.zrk`, records/value classes en `inline_values.zrk`, enums algebraicos en `algebraic_enums.zrk`, casts comprobados en `checked_casts.zrk`, `?.` simple y encadenado, `Iterable<T>` propio) — se sumaron en el camino de 13.5/13.6 los que realmente faltaban (`user_iterable.zrk`, `chained_safe_navigation.zrk`, `enum_class_payload.zrk`), sin relleno adicional que no agregue una combinación nueva
- [x] 13.2 Ampliar el corpus con programas inválidos, con snapshots de sus diagnósticos — auditado igual contra `crates/zirk-cli/tests/corpus/invalid/`; un hueco real encontrado y llenado: `abstract class` no tenía ningún programa en el corpus a pesar de estar completamente implementado a nivel de tipos (`abstract_class_not_lowered.zrk`, documenta que la declaración en sí es `NOT_LOWERED` — task 5.8). "Snapshot" aquí es lo que el arnés de `end_to_end.rs` ya verifica por cada entrada: código de salida distinto de cero, un diagnóstico `error[...]` con `= cause:`, y nada escrito a `stdout` — no hay comparación de texto exacto por archivo
- [x] 13.3 Verificar que el corpus de las fases 1 y 2 sigue verde sin modificarlo — `git status` sobre `crates/zirk-cli/tests/corpus/` confirma el único archivo tocado en toda la sesión es `invalid/later_phase.zrk` (edición ya documentada, propia de esta fase: pasó de probar un `record` liso a uno genérico porque el liso ya baja); ningún archivo de fase 1/2 fue modificado y los 705 tests del workspace, que incluyen todo el corpus, están en verde
- [x] 13.4 Test de que `"a" + "b"` concatena, cerrando la deuda de la fase anterior — `crates/zirk-cli/tests/corpus/valid/string_concatenation.zrk`, end-to-end con LLVM real
- [x] 13.5 Test de `for ... in` sobre un tipo propio que implementa `Iterable<T>` — bajaba a `NOT_LOWERED` en tres sitios distintos (`for ... in` sobre `Base::Class` en `element_type`, `implements Iterable<T>` en `resolve_implements_args`, `Iterator<T>`/`Iteration<T>` escritos como tipo en `resolve_contract_reference`/`resolve_enum_reference`); los tres se relajan ahora, pero solo para el trío nativo (`self.native_iteration`), no para un contrato o enum genérico cualquiera — esos siguen gateados, ya que nada más especializa la tabla de contratos propia de una clase o el layout de otro enum genérico todavía.
  Checker: `element_type` interna la instanciación de `Iteration<T>` que un `for ... in` concreto necesita (`intern_enum_instance`, igual que `Box<Int32>` se interna al construirse) y la registra en `CheckedProgram.for_in_iteration` (span del iterable → id en `enum_instances`), porque nada más deriva esa información del AST — no hay `T` escrito en ningún lado del `for ... in` en sí. `check_variant_construction` gana el mismo tratamiento que `check_construction` le dio a una clase genérica en 11.1: infiere `T` desde los argumentos (`Iteration.Item(value: v)` infiere `T` de `v`), interna la instanciación y la registra en `CheckedProgram.variant_constructions` (span de la llamada → id). Encontrado en el camino: `check_variant_construction` nunca traía sus propios `type_params` a la firma sintética que arma (siempre `Vec::new()`), así que ningún enum genérico podía construir una variante con datos sin `TYPE_MISMATCH` — el mismo hallazgo real que bloqueaba `Box(5)` en 11.1, aquí sin cerrar hasta ahora porque hasta esta tarea `Iteration<T>` era el único enum genérico que un programa podía siquiera nombrar.
  IR: especialización de enums genéricos (`specialize_enum`, paralela a `specialize_class` de 11.1) — un `EnumLayout` concreto por cada entrada de `checked.enum_instances`, apendeada tras las clases (`enum_instance_base`, el mismo patrón que `instance_base`); `ir_type` gana los casos `Base::ContractInstance` (mapea directo al contrato base, sin especializar — la tabla de despacho nunca dependió de `T`, solo de la clase y el índice) y `Base::EnumInstance` (mapea a `enum_instance_base + id`). `lower_for_in_iterable` (nueva, junto a la rama de rango existente) baja el protocolo completo: `it.iterator()` una vez, luego `next()` en la cabecera de cada vuelta, comparado por discriminante contra `Item`/`Done` igual que un `match` lo haría — extrae el valor asociado y entra al cuerpo, o sale.
  Tres hallazgos reales encontrados y arreglados en el camino, todos preexistentes: (1) los dos internamientos incondicionales que `register_native_iteration_contracts` hace para las firmas nativas (`iterator(): Iterator<T>`, `next(): Iteration<T>`) quedan en la tabla con argumentos aún sin resolver (`Base::Param`) — la especialización de enums los procesaba igual que una instancia real y crasheaba `ir_type` con `unreachable!` en **todo** programa, tuviera o no `Iterable` de por medio; ahora se detectan (cualquier campo asociado que siga siendo `Base::Param` tras la sustitución) y reciben el mismo placeholder vacío que una plantilla genérica. (2) un acceso a variante sin paréntesis (`Iteration.Done`) siempre bajaba como `ConstInt`/`IrType::Int32` puro, sin importar si el enum al que pertenece tiene datos en otra variante — un bug real y preexistente para *cualquier* enum algebraico con una variante vacía (`Shape.Empty` junto a `Shape.Circle(radius: Int32)`), nunca antes ejercido porque ningún test lo probaba; `lower_field`/`field_type_of` ahora construyen el valor completo (`BuildEnum` con discriminante y sin campos) cuando el enum tiene payload en algún lado. (3) para el caso genérico específicamente, ese mismo acceso resuelve por nombre bajo el id de la plantilla (vacío) en vez de la especialización concreta — `lower_expr_as` gana un caso que, cuando el destino ya conoce un `IrType::Enum` concreto (retorno, asignación), construye directamente contra ese id en vez de dejar que la resolución por nombre encuentre la plantilla; es la contraparte, dirigida por el destino, de `bare_enum_matches_instance` en el checker (que ya aceptaba esta combinación de tipos sin resolver a qué instancia concreta pertenecía).
  Verificado con un programa real compilado y corrido (`crates/zirk-cli/tests/corpus/valid/user_iterable.zrk`): una clase `Counter implements Iterable<Int32>` cuyo `iterator()` construye una `CounterIterator implements Iterator<Int32>`, cuyo `next()` alterna entre `Iteration.Item(value: ...)` e `Iteration.Done` según el estado interno — el `for ... in` recorre los cuatro valores y termina limpio.
- [x] 13.6 Sondear los cruces entre construcciones, no solo cada una por separado — probados con programas reales compilados y corridos: genérico + nulable + `?.` sobre un método (`Box<Int32>?`), record como campo de una clase genérica, `extends` + contrato + cast comprobado + `?.` sobre contrato en la misma cadena de despacho, parámetro de tipo con `from` llamando un método de la restricción directamente (sin `?.`), closure capturando `this` dentro del método de una clase genérica, cast entre dos instanciaciones distintas del mismo genérico (`Box<Int32> as Box<String>`, rechazado como error de tipos genuino, sin crash), y `Iterable<Box<T>>` (un `for ... in` cuyo elemento es a su vez una instancia genérica). Todos pasaron sin cambios.
  Dos hallazgos reales, ambos preexistentes y nunca antes ejercidos, arreglados en el camino: (1) un dato asociado de un enum no podía nombrar una clase (`Event.Login(user: User)`) sin importar en qué orden del archivo apareciera — `declare_enum` corría antes de que cualquier clase se registrara; corregido registrando el nombre y la aridad de cada clase (`register_class`, que no resuelve ningún tipo por sí solo) antes de que los enums resuelvan sus propios campos, sin tocar el resto del orden de tres pasadas que las clases ya usaban. (2) `is_reference_type`, que decide dónde aplica la matriz `mut`/`inmut`/`inmut::strict` (D11, task 5.15), nunca reconocía `Base::Instance` — la instancia especializada de una clase genérica volvía una `inmut::strict` completamente transparente a un alias mutable, sin ningún diagnóstico; corregido resolviendo la clase real detrás de la instanciación (`generic_instances[id].class`) y aplicando la misma regla que ya regía para una clase ordinaria, y sumado `Base::ContractInstance` al lado de `Base::Contract` que ya estaba.
  Corpus nuevo: `enum_class_payload.zrk`, `chained_safe_navigation.zrk` (válidos), `strict_alias_violation_generic.zrk` (inválido, cubre el segundo hallazgo).
- [ ] 13.7 Confirmar que CI pasa en las cuatro plataformas de la matriz

## 14. Cierre

- [ ] 14.1 Actualizar `docs/init/ZIRK_AGENT_PROMPT.md` con el estado de la fase
- [ ] 14.2 Registrar en ADRs las decisiones durables: layout de objetos y forma del despacho
- [ ] 14.3 Resolver o registrar como pendientes las preguntas abiertas del design
- [ ] 14.4 Revisar qué deudas de fases anteriores quedan vivas y con qué fecha
- [ ] 14.5 Confirmar el límite de Fase 3 de D9 sin presentarlo como semántica final: ninguna closure escapa ni se anota todavía
